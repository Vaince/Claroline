{% extends "ICAPDropZoneBundle:DropZone:correct.html.twig" %}
{% block dropzone_content %}
    <h3>
        Questionnaire d'évaluation :
    </h3>

    <div class="container">
        <form
        {% for criterion in dropZone.peerReviewCriteria %}
            <div class="row form-group">
                <div class="col-md-8">
                    {{ criterion.instruction }}
                </div>
                <div class="col-md-4">
                    {% for i in 1..dropZone.totalCriteriaColumn %}
                        <input type="radio" name="{{ criterion.id }}" value="{{ i }}"/>
                    {% endfor %}
                </div>
            </div>
        {% endfor %}
    </div>

{% endblock %}

{% block javascripts %}
    {{ parent() }}}
{% endblock %}